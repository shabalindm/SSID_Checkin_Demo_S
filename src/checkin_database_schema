//This is a schema of ssidcheckin database

CREATE TABLE PERSON (
	USER_ID VARCHAR(20) NOT NULL,
	SSID_LIST VARCHAR(10000) NOT NULL,
	PRIMARY KEY (USER_ID)
);

CREATE TABLE WIRELESS_SPOT (
	SSID VARCHAR(20) NOT NULL,
	CHECKIN_LIST VARCHAR(10000) NOT NULL,
	PRIMARY KEY (SSID)
);